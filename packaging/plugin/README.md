# Project Cognition plugin package

This skills-only plugin contains the standalone `project-cognition` skill under `skills/project-cognition/`.

For direct installation into `~/.agents/skills`, use the standalone skill ZIP. Use this package for plugin- or marketplace-based installation.
